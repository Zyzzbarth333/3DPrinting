# Normalise text to LF. Klipper runs on Linux, so configs must stay LF.
* text=auto eol=lf
*.cfg text eol=lf
*.ini text eol=lf
*.md  text eol=lf

# Binaries (if any are added later)
*.stl binary
*.3mf binary
*.png binary
*.jpg binary
*.zip binary